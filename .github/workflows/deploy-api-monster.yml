name: Deploy Password Manager (MonsterASP)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: windows-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore src/PasswordManager.Api/PasswordManager.Api.csproj

      - name: Build
        run: dotnet build src/PasswordManager.Api/PasswordManager.Api.csproj -c Release --no-restore

      - name: Deploy using publish profile
        env:
          WEBDEPLOY_PASSWORD: ${{ secrets.WEBDEPLOY_PASSWORD }}
        run: |
          dotnet publish src/PasswordManager.Api/PasswordManager.Api.csproj `
            -c Release `
            /p:PublishProfile=site56937-WebDeploy `
            /p:Password=$env:WEBDEPLOY_PASSWORD
